name: Build Panihari APK
on:
  push:
    branches: [panihari-apk-build]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'
      - name: Build debug APK
        run: gradle assembleDebug --stacktrace
      - name: Rename APK
        run: cp app/build/outputs/apk/debug/app-debug.apk Panihari-Orders-Printer-v1.apk
      - uses: actions/upload-artifact@v4
        with:
          name: Panihari-Orders-Printer-APK
          path: Panihari-Orders-Printer-v1.apk
          retention-days: 7
